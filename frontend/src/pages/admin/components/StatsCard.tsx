import React from 'react'

interface StatsCardProps {
  type: String,
  total: number,
}

export const StatsCard = ({ type, total } : StatsCardProps) => {
  return (
    <div className=''>
      
    </div>
  )
}
